# --- SECRETS / SESSION: never publish ---
# edi-gla persistent login (cookies + storage = live credentials)
.edigla-profile/
# rzjets persistent profile (holds the Cloudflare clearance cookie)
.rzjets-profile/
# exploratory dumps (HTML/screenshots of the logged-in site, other tails' plans)
edigla_capture/
edigla_out/
rzjets_out/

# --- generated / derived (rebuilt from fleet.json via build_sheet.py) ---
SimBrief_B777_Fleet.xlsx

# --- CI estimator generated/local data + rendered viz (never publish) ---
ci_samples.jsonl
route_cache.json
ci_viz.html

# --- delivery archives ---
*.zip

# --- python / uv ---
__pycache__/
*.py[cod]
.venv/
.python-version

# --- editor / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
